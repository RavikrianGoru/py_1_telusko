# The following table gives precedence order form lower(least binding) to higher(most binding).
# lambda : Lambada expression
# if - else
# or
# and
# not x
# in, not in, is, is not, <, <=, >, >=, !=, ==
# |
# ^
# &
# << , >>
# +, -
# *, /, //, %
# +x, -x, ~x
# **
# x[index], x[index:index], method(argument..), x.attribute
# (extression...), [expressions...], {key:value, .....}, {expressions...}

# operators which are same precedence associates from left to right.

